{
  "description": "cache-tax (Mod): session.start, classic.SessionStart, command.run{command=keepwarm|cache-tax}, prompt.submit, turn.step, turn.complete, session.compact. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": [
    "./register.ts"
  ]
}
